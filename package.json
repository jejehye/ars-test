{
  "name": "ars-test",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "extract": "node tools/extract-menu.mjs",
    "dev": "npm run extract && vite --host",
    "build": "npm run extract && tsc -b && vite build",
    "preview": "vite preview --host",
    "test": "vitest run",
    "sync": "npm run build && cap sync",
    "serve": "npm run build && vite preview --host --port 4173"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "fflate": "^0.8.2",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^2.1.8"
  }
}
